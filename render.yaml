services:
  - type: web
    name: heatguard-ai
    env: node
    plan: free
    buildCommand: cd frontend && npm install --include=dev && npm run build && cd ../node-backend && npm install
    startCommand: node node-backend/server.js
    envVars:
      - key: NODE_ENV
        value: production

